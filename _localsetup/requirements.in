# Human-maintained Python dependency input for framework tools.
# Runtime installs must stay isolated in a managed venv; use pipx for CLI apps,
# not for these importable framework libraries.
PyYAML>=6.0
requests>=2.28
python-frontmatter>=1.1
cryptography>=42.0
PGPy>=0.5.4,<0.6
jsonschema>=4.0
