# QC Patrol Prompt

Return strict JSON matching `llm-review.schema.json`.

Look for actionable repository maintenance risks in the supplied inventory, deterministic findings, snippets, and policy context. Do not include secrets, raw credentials, or endpoint URLs in output.
