#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

if [[ "${LOCALSETUP_ALLOW_PRIVATE_STAGE:-0}" != "1" ]]; then
  private_staged=()
  while IFS= read -r -d '' path; do
    case "$path" in
      .agents/state/*|.codex/runs/*|.codex/sessions/*|.codex/logs/*|.codex/tmp/*|.codex/*.backup|\
      .agent/*|.git-state-snapshots/*|.localsetup/*|localsetup.lock.json|\
      .localsetup-maint/config/*|.localsetup-maint/docs/*|.localsetup-maint/prompts/*|.localsetup-maint/reports/*|.localsetup-maint/state/*|\
      data/*|docs/*|graphify-out/*|_internal_docs/*|ls/.cache/*|ls/logs/*)
        private_staged+=("$path")
        ;;
    esac
  done < <(git diff --cached --name-only -z)

  if ((${#private_staged[@]})); then
    echo "[LocalSetup] refusing to commit private/local maintenance paths:"
    printf '  %s\n' "${private_staged[@]}"
    echo "[LocalSetup] move private work to ignored state, or set LOCALSETUP_ALLOW_PRIVATE_STAGE=1 only after an explicit public-boundary review."
    exit 1
  fi
fi

python3 ls/tools/localsetup.py hook-gate --out /tmp/localsetup-public.tar.gz >/tmp/localsetup-hook.json

if python3 - <<'PY'
import json
from pathlib import Path

payload = json.loads(Path("/tmp/localsetup-hook.json").read_text(encoding="utf-8"))
raise SystemExit(0 if payload.get("ok") else 1)
PY
then
  :
else
  echo "[LocalSetup] private path leak detected in package output"
  exit 1
fi

generated_paths=(
  ls/docs/_generated/artifact-registry.json
  ls/docs/_generated/workflow-catalog.json
  ls/docs/SKILLS.md
  ls/docs/_generated/facts.json
  ls/docs/_generated/docs-inventory.json
  ls/docs/_generated/docs-truth-map.json
  ls/docs/_generated/docs-audit-result.json
  ls/docs/_generated/docs-asset-manifest.json
  ls/docs/_generated/docs-alignment-summary.md
  ls/docs/_generated/implementation-file-map.md
  ls/docs/_generated/platform-adapters.md
  ls/docs/_generated/skill-packs.md
  ls/docs/_generated/skill_aliases.json
  ls/docs/migration/skill-alias-map.md
)

if ! git diff --quiet -- "${generated_paths[@]}"; then
  if [[ "${LOCALSETUP_HOOK_NO_STAGE:-0}" == "1" ]]; then
    echo "[LocalSetup] generated docs changed"
    exit 1
  fi
  git add "${generated_paths[@]}"
fi
