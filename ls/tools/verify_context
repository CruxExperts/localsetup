#!/usr/bin/env bash
# LocalSetup - Context verification. Thin wrapper; logic in verify_context.py.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
ENGINE_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"
exec python3 "$ENGINE_DIR/tools/verify_context.py"
