# Release Intelligence

Release intelligence is optional enrichment. Use GitHub Releases and repository metadata to summarize:

- latest release tag and date
- package or runtime hints visible in metadata or docs
- maintenance signals such as archived status, default branch, and recent pushes

If releases are missing, private, or rate-limited, record `unknown` instead of guessing.
