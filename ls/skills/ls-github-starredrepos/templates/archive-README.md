# Starred Repositories

This archive is generated from authenticated GitHub starred repository metadata.

See `manifest.json` for the machine-readable inventory and `docs/repos/` for per-repository notes.
