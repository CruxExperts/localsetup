# Managed by Ansible. Root-readable because it can contain service credentials.
{% for key, value in agent_host_secret_env_vars | dictsort %}
{{ key }}="{{ value | string | replace('\\', '\\\\') | replace('"', '\\"') | replace('\n', '\\n') }}"
{% endfor %}
