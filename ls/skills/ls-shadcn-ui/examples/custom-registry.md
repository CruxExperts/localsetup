# Custom Registry Example

Read first: `references/registry.md` and `references/mcp.md`.

Configure registry URLs, headers, and params using environment variables for
secrets. Preview items with `view` and `add --dry-run` before installation.

Failure modes: committing tokens, trusting community registry code as official,
and installing items without checking target alias placeholders.
