# Icon Rules

- Read `config.iconLibrary` before adding imports.
- Do not assume `lucide-react`.
- Use project-local icon wrapper patterns when present.
- Keep decorative icons `aria-hidden`.
- Give icon-only buttons labels with visible text, `aria-label`, or an existing
  accessible-name pattern.
- Match icon sizing with `size-*` utilities and button variants.
